<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>nb114514</groupId>
    <artifactId>AntiBitch</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>AntiBitch</name>

    <properties>
        <java.version>25</java.version>
        <paperapi.version>26.2.build.24-alpha</paperapi.version>
        <mc.api-version>26.2</mc.api-version>
        <build.classifier>26.2</build.classifier>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <finalName>${project.artifactId}-${project.version}-${build.classifier}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.2.4</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <profiles>
        <!-- 默认（无 profile 或 -P 26.2）：Minecraft 26.2 / Java 25 -->
        <profile>
            <id>26.2</id>
            <properties>
                <java.version>25</java.version>
                <paperapi.version>26.2.build.24-alpha</paperapi.version>
                <mc.api-version>26.2</mc.api-version>
                <build.classifier>26.2</build.classifier>
            </properties>
        </profile>
        <!-- -P 1.21.11：Minecraft 1.21.11 / Java 21，产出兼容老版本运行时的 jar -->
        <profile>
            <id>1.21.11</id>
            <properties>
                <java.version>21</java.version>
                <paperapi.version>1.21.11-R0.1-SNAPSHOT</paperapi.version>
                <mc.api-version>1.21.11</mc.api-version>
                <build.classifier>1.21.11</build.classifier>
            </properties>
        </profile>
    </profiles>

    <repositories>
        <repository>
            <id>papermc-repo</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>sonatype</id>
            <url>https://oss.sonatype.org/content/groups/public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paperapi.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
